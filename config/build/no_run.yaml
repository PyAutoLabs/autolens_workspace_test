# Scripts to skip during automated runs (smoke tests, pre-release checks, CI).
# Each entry is matched against script paths:
#   - Entries with '/' do a substring match against the file path
#   - Entries without '/' match the file stem exactly
# Add an inline # comment to document the reason for skipping.
#
# SLOW-skip convention:
#   Entries tagged `# SLOW <YYYY-MM-DD> - <reason>` mark scripts that are
#   skipped because they exceed the per-script timeout cap (300s by
#   default; 1800s for mode=release runs). These are
#   NOT permanent skips — every mega-run surfaces them with a loud warning
#   banner. Fix the performance issue and remove the SLOW marker.
#
# NEEDS_FIX convention:
#   Entries tagged `# NEEDS_FIX <YYYY-MM-DD> - <reason>` mark scripts that
#   are broken and parked as a to-do list. Like SLOW-skips, these are NOT
#   permanent skips — every mega-run surfaces them with a loud warning
#   banner. Investigate the failure, fix the underlying bug, and remove
#   the NEEDS_FIX marker.
#
# OPTIONAL-DEP convention:
#   Entries tagged `# OPTIONAL-DEP <YYYY-MM-DD> - <reason>` mark scripts that
#   are correct as-is but import a third-party package that is deliberately
#   NOT in the PyAuto install set (so it is absent from smoke / release
#   validation environments). Not a to-do marker and not surfaced as a
#   warning banner: the script stays on disk and is run by hand in an
#   environment that has the dependency. Remove the entry if the dependency
#   ever joins the validated install set.

- point_source/visualization/modeling_visualization_jit # SLOW 2026-07-08 - JIT + Part-2 live Nautilus fit exceeds 300s cap; same family as imaging/interferometer modeling_visualization_jit (the zero_contour perf-assert false-positive was separately fixed to a cold/warm ratio, which now lets the script run past it into the slow fit)
- cluster/visualization # SLOW 2026-07-21 - per-plane critical-curve + caustic computation on the required full-extent 250x250 viz_grid (multi-plane marching squares) totals ~580s, over the 300s cap; same perf family as the modeling_visualization_jit scripts. Curves DO recover (plane-1 7 CC, plane-2 1 CC at full data) and the per-plane physics assertion passes — this is NOT the mislabelled "#1280 zero_contour algorithmic regression", which does not reproduce. profile_smoke.yaml unsets FAST_PLOTS/SMALL_DATASETS so it runs green in manual/full runs.
- imaging/jax_likelihood/mge_group # NEEDS_FIX 2026-08-24 - higher-cap retime settled it: capped 3/3 at 1800s on BOTH Python legs (run 32758924176), on top of 5/5 at 300s in phase 2 (run 32664682689) -- 16/16 lifetime executions, zero completions. The log shows vmap+JIT compile finishing in ~16s and then nothing for the remaining ~1784s: a deterministic hang, not slowness. Owned by the stall bug (PyAutoMind draft/bug/ci/jax_vmap_jit_compile_stall.md -- the "compile stall" misnomer note applies; the stack sits in jax.block_until_ready). Not performance; do not raise the cap further (autolens_workspace_test#274).
# The rest of the PyAutoHeart#74 "flakes at the 1800s cap" SLOW family was retimed
# on 2026-08-24 (5 repeats per Python leg, 300s cap; runs 32741371308 + 32741386752,
# phase-2 run 32664682689). Eight entries completed 5/5 on both legs (slowest 54.5s)
# and were removed — marker refuted, scripts back in mega-run coverage. Two entries
# failed deterministically instead of running slow, so they were NEEDS_FIX not SLOW;
# interferometer/jax_likelihood/delaunay.py was fixed on 2026-08-24 (its NaN probe now
# poisons a valid instance below the model-construction boundary instead of building
# one from an all-NaN vector, which validate_ell_comps rejects) and is back in
# coverage. The one below remains. See autolens_workspace_test#274.
- interferometer/jax_grad/gradient.py # NEEDS_FIX 2026-08-24 - 3.13-only deterministic failure, 5/5 (run 32741386752): the RectangularRTUAdaptDensity + reg.Adapt sparse-operator check fails assert_eager_jit_consistent, eager (-3164.0196392095145) vs jitted (-3164.021216643465) disagreeing ~5e-7 rel — possible pure_callback constant-folding. 3.12 fully green at 61.2s (7% of its 900s cap). Not performance; see PyAutoMind draft/bug/autolens_workspace_test/gradient_eager_jit_divergence_py313.md

# BOOTSTRAP-TARGET simulators (NOT to-do markers — correct as-is). These scripts
# write dataset/ fixtures that are no longer committed (git rm'd, gitignored); the
# consuming scripts provision them on demand via the should_simulate()/`.exists()`
# bootstrap (subprocess into these). They are invoked *through* their consumers,
# never as standalone validated test entries — running them standalone in a
# mega-run would only re-emit runtime-generated data. Skip the standalone run.
- imaging/simulator/simple.py # BOOTSTRAP-TARGET - produces dataset/imaging/jax_test (imaging consumers)
- imaging/simulator/dspl.py # BOOTSTRAP-TARGET - produces dataset/imaging/jax_test_dspl (rectangular_dspl)
- imaging/simulator/no_lens_light.py # BOOTSTRAP-TARGET - produces dataset/build/imaging/no_lens_light (database/scrape general, scaling_relation)
- imaging/simulator/with_lens_light.py # BOOTSTRAP-TARGET - produces dataset/build/imaging/with_lens_light (convolution, model_fit, visualization, slam_general_jax, slam_pix_jax)
- interferometer/simulator/simple.py # BOOTSTRAP-TARGET - produces dataset/interferometer/simple (interferometer consumers)
- interferometer/simulator/dspl.py # BOOTSTRAP-TARGET - produces dataset/interferometer/dspl (rectangular_dspl)
- interferometer/simulator/no_lens_light.py # BOOTSTRAP-TARGET - produces dataset/build/interferometer/no_lens_light (interferometer/visualization, multi_dataset/visualization_interferometer)
- interferometer/simulator/with_lens_light.py # BOOTSTRAP-TARGET - produces dataset/build/interferometer/with_lens_light (interferometer/model_fit)
- multi_dataset/simulator.py # BOOTSTRAP-TARGET - produces dataset/multi_dataset/lens_sersic (multi consumers, multi_analysis_jax, slam_multi_one_by_one_jax, visualization_imaging)
- point_source/simulators/simple.py # BOOTSTRAP-TARGET (protective) - the committed dataset/point_source/simple is NOT simulator-reproducible (regeneration yields a different likelihood: point.py/image_plane.py assert the exact literal -83.38049778 tuned to the committed bytes). It stays committed (allowlisted); skip the standalone run so a mega-run cannot overwrite the reference data. The consumers' dormant should_simulate bootstrap only fires if the committed data is ever absent.
- point_source/simulators/point_source.py # BOOTSTRAP-TARGET - produces dataset/build/point_source (orphan; no current consumer, but a simulator not a standalone test)

# CULL demotes (issue #211, human decision 2026-07-24 — "keep only model_fit").
# Correct-as-is scripts whose recurring-run coverage is redundant with library
# unit tests or the validated user workspace; kept on disk for development.
- tracer_multiplane # CULL-DEMOTE 2026-07-24 - multiplane ray-tracing covered by PyAutoLens unit tests; dev-reference only (#211)
- misc/interop/coolest_round_trip.py # CULL-DEMOTE 2026-07-24 - COOLEST round-trip duplicated by validated autolens_workspace guides/coolest_interop.py; herculens parity sibling KEPT (#211)
- cluster/csv_api.py # CULL-DEMOTE 2026-07-24 - regenerates the committed hand-authored dataset/cluster/test CSVs consumed directly by likelihood_sanity; identically-named validated user script covers the API (#211)
- cluster/simulator.py # CULL-DEMOTE 2026-07-24 (protective) - stage 2 of the csv_api chain over hand-authored inputs; committed dataset/cluster/test carries calibrated parity consumers, skip so a mega-run cannot overwrite it (#211)

# OPTIONAL-DEP skips — correct-as-is scripts whose third-party imports are not
# in the validated install set.
- misc/interop/coolest_herculens_parity.py # OPTIONAL-DEP 2026-07-24 - imports `herculens` (and `coolest`), neither of which is a PyAutoLens dependency nor in the release-validation install set; mode=release fails at `ModuleNotFoundError: No module named 'herculens'`. Script is correct and stays on disk (KEPT by #211) — run it by hand in an environment with herculens installed.
- misc/database/scrape/multi_analysis_jax # SLOW 2026-07-30 - re-measured: times out at the real 300s cap (full search, no test mode)
- misc/database/scrape/slam_general_jax # SLOW 2026-07-30 - re-measured: times out at the real 300s cap (full search, no test mode)
- misc/database/scrape/slam_multi_one_by_one_jax # SLOW 2026-07-30 - re-measured: times out at the real 300s cap (full search, no test mode)
- misc/database/scrape/slam_pix_jax # SLOW 2026-07-30 - re-measured: times out at the real 300s cap (full search, no test mode)
- imaging/visualization/modeling_visualization_jit # SLOW 2026-07-30 - re-measured: times out at the 300s cap; JIT + full visualization pipeline
- imaging/visualization/modeling_visualization_delaunay_jit # NEEDS_FIX 2026-07-30 - now FAILS its own JIT-cache assertion at 52s (cached 2.45s not < 0.5x compile 2.72s) - possible closure cache-busting; see bug prompt jit_cache_not_hit_modeling_visualization
- imaging/visualization/modeling_visualization_rectangular_jit # NEEDS_FIX 2026-07-30 - now FAILS its own JIT-cache assertion at 36s (cached 2.21s not < 0.5x compile 2.28s) - same as delaunay variant
- interferometer/visualization/modeling_visualization_jit # SLOW 2026-07-30 - historically >300s cap; local re-measurement OOM-killed (SIGKILL) at 132s - memory-heavy interferometer JIT
- imaging/subhalo_recovery_evidence.py # SLOW 2026-07-31 - evidence-grid acceptance test of PyAutoLens#672 (156-point one-shot + 25-point iterative-to-convergence scan): ~1-2h by design; run by hand, results recorded on the issue. Deliberately a manual validation script, not a smoke/release entry - its 300s-scale sibling imaging/subhalo_recovery.py stays the automated gate.
- multi_dataset/jax_likelihood/delaunay.py # NEEDS_FIX 2026-08-01 - hangs to the 1800s release cap in 3 of 5 release-integrate runs since 07-31 (18s when it passes; intermittent XLA compile stall, family-wide - ag_test rectangular.py also hit); quarantined to unblock the 2026-08-01 release; see autolens_workspace_test#245
